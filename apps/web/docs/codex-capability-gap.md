# Codex Capability Baseline

The current source-level and verified runtime baseline is maintained at
[`../../../docs/capability-baseline.md`](../../../docs/capability-baseline.md).

The files under `apps/web/contracts/codex` remain contract-harness inputs until
they are replaced by a bundle generated from the pinned Codex build. They are
not an observed server Manifest.
