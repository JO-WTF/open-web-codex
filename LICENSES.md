# Licensing map

This repository combines independently licensed components:

- `apps/web/**` retains the license in `apps/web/LICENSE`.
- `codex/**` retains the Apache-2.0 license and notices in `codex/LICENSE` and
  `codex/NOTICE`.
- New root-level orchestration, documentation, and sync scripts do not change the
  license terms of either imported component.

Before distributing a combined binary or container, review both component
licenses and include all required notices.
