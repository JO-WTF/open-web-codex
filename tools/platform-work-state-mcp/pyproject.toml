[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "open-web-codex-platform-work-state-mcp"
version = "0.1.0"
description = "Signed Work State mutation MCP for authorized domain Agents"
requires-python = ">=3.11"
dependencies = ["mcp>=1.10,<2"]

[tool.setuptools]
py-modules = ["server"]
