分析现有印尼仓库网络，并判断能否从完整的已审核候选点中新增一个前置仓，使两日需求覆盖率达到 74%。请保持这个业务百分比口径不变。建设费用按 5 年摊销。

请比较当前方案与入选方案的一日、两日、三日需求覆盖率，以及干线、末端、运输总成本、年度固定成本、摊销建设成本和年度决策总成本，并创建地图展示两者差异。

根据尚未解决的证据问题动态选择必要 Agent。只使用平台授权的数据和类型化 Artifact，不读取原始客户行、不自行编造候选地点。最终通过平台验证的 `report.v1` 交付确定性决策报告，并在其后交付独立的 `map.v3`；不要让模型复制或改写报告正文。
